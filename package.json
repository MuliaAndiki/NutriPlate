{
  "name": "nutriplate",
  "private": true,
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "fe",
    "be",
    "api"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
